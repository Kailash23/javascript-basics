var mapuse = [1, 1, 1].map(function(num) {
	return num * 10;
});

console.log(mapuse);

/*
   10, 10, 10 ]
*/

var mapuse = [1, 1, 1].map(function(num) {
	return 'watch and code';
});

console.log(mapuse);

/*
  [ 'watch and code', 'watch and code', 'watch and code' ]
*/

var mapuse = [1, 1, 1].map(function(num) {
	return true;
});

console.log(mapuse);

/*
  [ true, true, true ]
*/
